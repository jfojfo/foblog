<li>
<h2>{{ title|default:"Links"|escape }}</h2>
<ul>
	{% for link in widget_links %}
	<li><a href="{{ link.url }}" title="{{ link.description|escape }}" target="_blank">{{ link.text }}</a></li>
	{% endfor %}
</ul>
